**数据可视化专项练习题（完整版·含可运行代码）**

**前言**

本文档匹配计划学习数据可视化的朋友，涵盖**Matplotlib基础绘图、图表美化、Seaborn统计图表、Plotly交互式可视化、地图可视化、综合项目全知识点**。所有题目均提供可直接执行的初始化代码和最优参考答案，建议先独立完成题目，再核对代码。

**一、通用前置准备**

所有练习题均基于以下环境配置，执行前请先运行该段代码，确保中文正常显示、结果可复现。

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
import numpy as np<br />
import pandas as pd<br />
import matplotlib.pyplot as plt<br />
import seaborn as sns<br />
import plotly.express as px<br />
<br />
# Matplotlib 全局中文显示配置<br />
plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei', 'DejaVu
Sans']<br />
plt.rcParams['axes.unicode_minus'] = False # 解决负号显示异常<br />
plt.rcParams['font.size'] = 12 # 全局基础字号<br />
<br />
# 设置随机种子，保证数据可复现<br />
np.random.seed(2026)</td>
</tr>
</tbody>
</table>

**第一部分 Matplotlib基础绘图（第36-37天）**

**第36天 对应知识点：折线图、柱状图、时间序列、对比图**

**初始化代码（先执行）**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
# 1. 生成函数曲线数据<br />
x = np.linspace(0, 2*np.pi, 100)<br />
y_sin = np.sin(x)<br />
y_cos = np.cos(x)<br />
<br />
# 2. 产品销量数据<br />
product_list = ["产品A", "产品B", "产品C", "产品D", "产品E"]<br />
sales_data = [120, 85, 160, 95, 130]<br />
<br />
# 3. 30天时间序列销售数据<br />
date_range = pd.date_range(start="2026-06-01", periods=30,
freq="D")<br />
daily_sales = 1000 + np.random.randn(30) * 150<br />
<br />
# 4. 双Y轴月度数据<br />
months = [f"{i}月" for i in range(1,13)]<br />
month_sales = np.random.randint(500, 2000, size=12)<br />
month_profit = np.random.uniform(0.1, 0.3, size=12)</td>
</tr>
</tbody>
</table>

**题目列表**

|      |                |                                                                                                              |
|------|----------------|--------------------------------------------------------------------------------------------------------------|
| 题号 | 题目类型       | 需求描述                                                                                                     |
| 1    | 单折线图       | 绘制sin(x)正弦函数曲线，设置x轴标签x值、y轴标签sin(x)、标题正弦函数曲线，开启浅色网格线，线条设为蓝色、线宽2 |
| 2    | 多折线对比图   | 同画布绘制sin(x)和cos(x)两条曲线，分别设为红色、蓝色，添加图例，限定x轴0\~2π、y轴-1.2\~1.2                   |
| 3    | 基础柱状图     | 绘制5个产品的销量垂直柱状图，每个柱子顶部标注具体数值，y轴从0开始，标题产品销量对比                          |
| 4    | 时间序列可视化 | 绘制30天销售额折线图，x轴日期标签旋转45°，标注销售额最高、最低的两个点位并配箭头                             |
| 5    | 双Y轴对比图    | 左轴用柱状图展示月度销量，右轴用折线图展示月度利润率，添加双轴标签和图例                                     |

**参考答案**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
# ---------- 题1：单折线图 ----------<br />
plt.figure(figsize=(8, 4))<br />
plt.plot(x, y_sin, color="blue", linewidth=2)<br />
plt.xlabel("x值")<br />
plt.ylabel("sin(x)")<br />
plt.title("正弦函数曲线")<br />
plt.grid(alpha=0.3, linestyle="--")<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题2：多折线对比图 ----------<br />
plt.figure(figsize=(8, 4))<br />
plt.plot(x, y_sin, color="red", linewidth=2, label="sin")<br />
plt.plot(x, y_cos, color="blue", linewidth=2, label="cos")<br />
plt.xlabel("x值")<br />
plt.ylabel("y值")<br />
plt.title("正弦与余弦函数对比")<br />
plt.legend()<br />
plt.xlim(0, 2*np.pi)<br />
plt.ylim(-1.2, 1.2)<br />
plt.grid(alpha=0.3)<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题3：基础柱状图 ----------<br />
plt.figure(figsize=(8, 4))<br />
bars = plt.bar(product_list, sales_data, color="#4C72B0")<br />
# 柱子顶部标注数值<br />
for bar in bars:<br />
height = bar.get_height()<br />
plt.text(bar.get_x() + bar.get_width()/2, height, f"{height}",<br />
ha="center", va="bottom", fontsize=10)<br />
plt.ylim(0, max(sales_data)*1.15)<br />
plt.title("产品销量对比")<br />
plt.ylabel("销量")<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题4：时间序列可视化 ----------<br />
plt.figure(figsize=(10, 4))<br />
plt.plot(date_range, daily_sales, color="#2E8B57", linewidth=2)<br />
plt.title("30天每日销售额趋势")<br />
plt.ylabel("销售额（元）")<br />
plt.xticks(rotation=45)<br />
plt.grid(alpha=0.3)<br />
<br />
# 标注最高/最低点<br />
max_idx = daily_sales.argmax()<br />
min_idx = daily_sales.argmin()<br />
# 最高点标注<br />
plt.annotate(f"销售额最高：{daily_sales[max_idx]:.0f}元",<br />
xy=(date_range[max_idx], daily_sales[max_idx]),<br />
xytext=(date_range[max_idx+3], daily_sales[max_idx]+50),<br />
arrowprops=dict(facecolor="red", shrink=0.05, width=1))<br />
# 最低点标注<br />
plt.annotate(f"销售额最低：{daily_sales[min_idx]:.0f}元",<br />
xy=(date_range[min_idx], daily_sales[min_idx]),<br />
xytext=(date_range[min_idx-5], daily_sales[min_idx]-100),<br />
arrowprops=dict(facecolor="green", shrink=0.05, width=1))<br />
<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题5：双Y轴对比图 ----------<br />
fig, ax1 = plt.subplots(figsize=(10, 4))<br />
# 左轴：销量柱状图<br />
bars = ax1.bar(months, month_sales, color="#4C72B0", alpha=0.7,
label="月度销量")<br />
ax1.set_ylabel("销量（件）", color="#4C72B0")<br />
ax1.tick_params(axis='y', labelcolor="#4C72B0")<br />
<br />
# 右轴：利润率折线图<br />
ax2 = ax1.twinx()<br />
line = ax2.plot(months, month_profit, color="#DD8452", linewidth=2,
marker="o", label="利润率")<br />
ax2.set_ylabel("利润率", color="#DD8452")<br />
ax2.tick_params(axis='y', labelcolor="#DD8452")<br />
ax2.set_ylim(0, 0.4)<br />
<br />
# 合并图例<br />
lines1, labels1 = ax1.get_legend_handles_labels()<br />
lines2, labels2 = ax2.get_legend_handles_labels()<br />
ax1.legend(lines1 + lines2, labels1 + labels2, loc="upper left")<br />
<br />
plt.title("月度销量与利润率对比")<br />
plt.tight_layout()<br />
plt.show()</td>
</tr>
</tbody>
</table>

**第37天 对应知识点：散点图、直方图、箱线图、子图布局、分布分析**

**初始化代码（先执行）**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
# 1. 正态分布散点数据<br />
scatter_x = np.random.randn(100)<br />
scatter_y = np.random.randn(100)<br />
<br />
# 2. 正态分布直方图数据<br />
hist_data = np.random.normal(loc=5, scale=2, size=1000)<br />
<br />
# 3. 三组对比箱线图数据<br />
box_data = [<br />
np.random.normal(2, 0.8, 200),<br />
np.random.normal(5, 1.0, 200),<br />
np.random.normal(8, 0.9, 200)<br />
]<br />
box_labels = ["组别1", "组别2", "组别3"]<br />
<br />
# 4. 分类散点数据<br />
classA_x = np.random.normal(2, 0.6, 50)<br />
classA_y = np.random.normal(3, 0.6, 50)<br />
classB_x = np.random.normal(5, 0.7, 50)<br />
classB_y = np.random.normal(6, 0.7, 50)<br />
<br />
# 5. 四种分布数据<br />
dist_norm = np.random.normal(0, 1, 500)<br />
dist_uniform = np.random.uniform(-3, 3, 500)<br />
dist_poisson = np.random.poisson(2, 500)<br />
dist_exp = np.random.exponential(1, 500)<br />
dist_list = [dist_norm, dist_uniform, dist_poisson, dist_exp]<br />
dist_names = ["正态分布", "均匀分布", "泊松分布", "指数分布"]</td>
</tr>
</tbody>
</table>

**题目列表**

|      |                |                                                                                          |
|------|----------------|------------------------------------------------------------------------------------------|
| 题号 | 题目类型       | 需求描述                                                                                 |
| 1    | 基础散点图     | 绘制100个标准正态分布散点，点大小25、透明度0.6、深绿色，标题随机正态分布散点图           |
| 2    | 频数直方图     | 绘制1000个正态分布数据的直方图，bins=20，柱子边框黑色，标注x/y轴标签                     |
| 3    | 多组箱线图     | 绘制三组数据的箱线图，x轴标注对应组别名称，标题三组数据分布对比箱线图                    |
| 4    | 2×2子图布局    | 2行2列画布分别绘制：正弦折线图、销量柱状图、正态散点图、分布直方图，每个子图加独立小标题 |
| 5    | 分类散点图     | 同画布绘制A、B两类散点，不同颜色区分，添加图例，标注两类数据的中心点坐标                 |
| 6    | 多分布对比子图 | 1行4列子图分别绘制四种分布的直方图，每个子图标注分布名称，统一y轴范围                    |

**参考答案**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
# ---------- 题1：基础散点图 ----------<br />
plt.figure(figsize=(6, 5))<br />
plt.scatter(scatter_x, scatter_y, s=25, alpha=0.6,
color="darkgreen")<br />
plt.title("随机正态分布散点图")<br />
plt.grid(alpha=0.3)<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题2：频数直方图 ----------<br />
plt.figure(figsize=(7, 4))<br />
plt.hist(hist_data, bins=20, edgecolor="black", color="#55A868",
alpha=0.8)<br />
plt.xlabel("数值区间")<br />
plt.ylabel("频数")<br />
plt.title("正态分布频数直方图")<br />
plt.grid(axis='y', alpha=0.3)<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题3：多组箱线图 ----------<br />
plt.figure(figsize=(7, 4))<br />
plt.boxplot(box_data, labels=box_labels, patch_artist=True,<br />
boxprops=dict(facecolor="#8172B2", alpha=0.7))<br />
plt.title("三组数据分布对比箱线图")<br />
plt.ylabel("数值")<br />
plt.grid(axis='y', alpha=0.3)<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题4：2×2子图布局 ----------<br />
fig, axes = plt.subplots(2, 2, figsize=(10, 8))<br />
<br />
# 子图1：正弦折线<br />
axes[0,0].plot(x, y_sin, color="red")<br />
axes[0,0].set_title("正弦函数曲线")<br />
axes[0,0].grid(alpha=0.3)<br />
<br />
# 子图2：销量柱状<br />
axes[0,1].bar(product_list, sales_data, color="#4C72B0")<br />
axes[0,1].set_title("产品销量对比")<br />
axes[0,1].tick_params(axis='x', rotation=30)<br />
<br />
# 子图3：散点图<br />
axes[1,0].scatter(scatter_x, scatter_y, alpha=0.6,
color="darkgreen")<br />
axes[1,0].set_title("正态分布散点")<br />
<br />
# 子图4：直方图<br />
axes[1,1].hist(hist_data, bins=15, edgecolor="black", alpha=0.7)<br />
axes[1,1].set_title("分布直方图")<br />
<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题5：分类散点图 ----------<br />
plt.figure(figsize=(7, 5))<br />
plt.scatter(classA_x, classA_y, s=30, alpha=0.7, color="#4C72B0",
label="类别A")<br />
plt.scatter(classB_x, classB_y, s=30, alpha=0.7, color="#DD8452",
label="类别B")<br />
<br />
# 标注中心点<br />
a_center_x, a_center_y = classA_x.mean(), classA_y.mean()<br />
b_center_x, b_center_y = classB_x.mean(), classB_y.mean()<br />
plt.scatter(a_center_x, a_center_y, s=100, color="blue", marker="*",
zorder=5)<br />
plt.scatter(b_center_x, b_center_y, s=100, color="red", marker="*",
zorder=5)<br />
plt.text(a_center_x+0.1, a_center_y,
f"中心({a_center_x:.1f},{a_center_y:.1f})")<br />
plt.text(b_center_x+0.1, b_center_y,
f"中心({b_center_x:.1f},{b_center_y:.1f})")<br />
<br />
plt.legend()<br />
plt.title("两类数据散点分布")<br />
plt.grid(alpha=0.3)<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题6：多分布对比子图 ----------<br />
fig, axes = plt.subplots(1, 4, figsize=(16, 3), sharey=True)<br />
for i in range(4):<br />
axes[i].hist(dist_list[i], bins=20, edgecolor="black", alpha=0.7,
color="#8172B2")<br />
axes[i].set_title(dist_names[i])<br />
axes[i].grid(axis='y', alpha=0.3)<br />
<br />
plt.suptitle("四种概率分布对比", y=1.05)<br />
plt.tight_layout()<br />
plt.show()</td>
</tr>
</tbody>
</table>

**第二部分 图表美化与样式配置（第38天）**

**对应知识点：中文显示、样式表、自定义配置、标注、专业排版**

**初始化代码（先执行）**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
# 复用前文的正弦曲线数据、产品销量数据<br />
x = np.linspace(0, 2*np.pi, 100)<br />
y_sin = np.sin(x)<br />
product_list = ["产品A", "产品B", "产品C", "产品D", "产品E"]<br />
sales_data = [120, 85, 160, 95, 130]</td>
</tr>
</tbody>
</table>

**题目列表**

|      |                  |                                                                                                         |
|------|------------------|---------------------------------------------------------------------------------------------------------|
| 题号 | 题目类型         | 需求描述                                                                                                |
| 1    | 样式表对比       | 分别使用ggplot、fivethirtyeight、dark_background三种内置样式绘制同一条sin曲线，观察视觉差异             |
| 2    | 关键点标注       | 绘制sin(x)曲线，在(π/2, 1)处标注「最大值」、(3π/2, -1)处标注「最小值」，均配箭头指向                    |
| 3    | 全局样式自定义   | 通过rcParams全局设置：默认线宽2、主色墨绿色、网格虚线、坐标轴标签字号14，绘制折线图验证效果             |
| 4    | 出版级柱状图美化 | 绘制销量柱状图，要求：去掉顶部/右侧边框、自定义渐变色、柱子顶部标数值、水平网格置于底层、排版整齐无溢出 |

**参考答案**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
# ---------- 题1：内置样式表对比 ----------<br />
style_list = ["ggplot", "fivethirtyeight", "dark_background"]<br />
for style in style_list:<br />
with plt.style.context(style): # 临时生效样式，不影响全局<br />
plt.figure(figsize=(6, 3))<br />
plt.plot(x, y_sin, linewidth=2)<br />
plt.title(f"当前样式：{style}")<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题2：关键点标注 ----------<br />
plt.figure(figsize=(8, 4))<br />
plt.plot(x, y_sin, color="#2E8B57", linewidth=2)<br />
<br />
# 最大值标注<br />
plt.annotate("最大值",<br />
xy=(np.pi/2, 1),<br />
xytext=(np.pi/2 + 0.5, 0.8),<br />
arrowprops=dict(facecolor="red", shrink=0.05, width=1))<br />
# 最小值标注<br />
plt.annotate("最小值",<br />
xy=(3*np.pi/2, -1),<br />
xytext=(3*np.pi/2 - 1.2, -0.7),<br />
arrowprops=dict(facecolor="blue", shrink=0.05, width=1))<br />
<br />
plt.ylim(-1.3, 1.3)<br />
plt.title("正弦曲线极值标注")<br />
plt.grid(alpha=0.3)<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题3：全局样式自定义 ----------<br />
# 临时修改全局配置<br />
with plt.rc_context({<br />
"lines.linewidth": 2,<br />
"lines.color": "#006400", # 墨绿色<br />
"grid.linestyle": "--",<br />
"axes.labelsize": 14,<br />
"axes.titlesize": 16<br />
}):<br />
plt.figure(figsize=(8, 4))<br />
plt.plot(x, y_sin)<br />
plt.xlabel("X轴")<br />
plt.ylabel("Y轴")<br />
plt.title("全局自定义样式效果图")<br />
plt.grid(True)<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题4：出版级柱状图美化 ----------<br />
plt.figure(figsize=(8, 4.5))<br />
ax = plt.gca()<br />
<br />
# 渐变色柱子<br />
colors = ["#e0f2fe", "#7dd3fc", "#0ea5e9", "#0284c7", "#0369a1"]<br />
bars = ax.bar(product_list, sales_data, color=colors, edgecolor="white",
linewidth=1.5)<br />
<br />
# 去掉顶部和右侧边框<br />
ax.spines["top"].set_visible(False)<br />
ax.spines["right"].set_visible(False)<br />
<br />
# 柱子顶部标注数值<br />
for bar in bars:<br />
height = bar.get_height()<br />
ax.text(bar.get_x() + bar.get_width()/2, height + 2, f"{height}",<br />
ha="center", va="bottom", fontsize=11, fontweight="medium")<br />
<br />
# 网格置于底层<br />
ax.yaxis.grid(True, alpha=0.3, linestyle="--")<br />
ax.set_axisbelow(True)<br />
<br />
ax.set_ylim(0, max(sales_data)*1.15)<br />
ax.set_title("产品销量统计", fontsize=14, pad=15)<br />
ax.set_ylabel("销量（件）")<br />
<br />
plt.tight_layout()<br />
plt.show()</td>
</tr>
</tbody>
</table>

**第三部分 Seaborn统计图表（第39-40天）**

**第39天 对应知识点：热力图、分布图、配对图、联合分布图**

**初始化代码（先执行）**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
# 加载seaborn内置数据集<br />
iris = sns.load_dataset("iris")<br />
tips = sns.load_dataset("tips")</td>
</tr>
</tbody>
</table>

**题目列表**

|      |                |                                                                                   |
|------|----------------|-----------------------------------------------------------------------------------|
| 题号 | 题目类型       | 需求描述                                                                          |
| 1    | 相关性热力图   | 计算iris数据集4个数值特征的相关系数矩阵，绘制热力图，格子显示系数数值，配色RdBu_r |
| 2    | 单变量分布图   | 绘制tips数据集total_bill列的直方图+核密度曲线；再绘制按sex分组的核密度对比图      |
| 3    | 多变量配对图   | 对iris数据集绘制pairplot，按species分类上色，观察不同品种的特征差异               |
| 4    | 联合分布分析   | tips数据集x=消费总额、y=小费，绘制kind=reg的jointplot，展示散点、回归线和边缘分布 |
| 5    | 箱线图叠加散点 | 按日期分组绘制消费总额箱线图，叠加stripplot展示原始数据点，商务风配色             |

**参考答案**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
# ---------- 题1：相关性热力图 ----------<br />
plt.figure(figsize=(7, 6))<br />
corr_matrix = iris.select_dtypes("number").corr()<br />
sns.heatmap(corr_matrix, annot=True, cmap="RdBu_r", center=0,<br />
square=True, linewidths=0.5, fmt=".2f")<br />
plt.title("鸢尾花特征相关性热力图")<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题2：单变量分布图 ----------<br />
# 直方图+核密度曲线<br />
plt.figure(figsize=(8, 4))<br />
sns.histplot(data=tips, x="total_bill", kde=True, color="#4C72B0")<br />
plt.title("消费总额分布直方图")<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# 按性别分组核密度对比<br />
plt.figure(figsize=(8, 4))<br />
sns.kdeplot(data=tips, x="total_bill", hue="sex", fill=True, alpha=0.4,
linewidth=1.5)<br />
plt.title("男女消费金额分布对比")<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题3：多变量配对图 ----------<br />
sns.pairplot(data=iris, hue="species", palette="Set2",
corner=True)<br />
plt.suptitle("鸢尾花多特征配对图", y=1.02)<br />
plt.show()<br />
<br />
# ---------- 题4：联合分布分析 ----------<br />
g = sns.jointplot(data=tips, x="total_bill", y="tip", kind="reg",<br />
color="#55A868", height=6, ratio=5)<br />
g.fig.suptitle("消费总额与小费回归关系", y=1.02)<br />
plt.show()<br />
<br />
# ---------- 题5：箱线图叠加散点 ----------<br />
plt.figure(figsize=(9, 5))<br />
sns.boxplot(data=tips, x="day", y="total_bill", palette="Set2",
width=0.5)<br />
sns.stripplot(data=tips, x="day", y="total_bill", color="black",<br />
size=4, alpha=0.5, jitter=0.2)<br />
plt.title("每日消费金额分布（箱线+原始散点）")<br />
plt.ylabel("消费总额（美元）")<br />
plt.tight_layout()<br />
plt.show()</td>
</tr>
</tbody>
</table>

**第40天 对应知识点：回归图、分类图、小提琴图、分面网格**

**初始化代码（先执行）**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
tips = sns.load_dataset("tips")<br />
anscombe = sns.load_dataset("anscombe")</td>
</tr>
</tbody>
</table>

**题目列表**

|      |                    |                                                                          |
|------|--------------------|--------------------------------------------------------------------------|
| 题号 | 题目类型           | 需求描述                                                                 |
| 1    | 线性回归图         | tips数据集x=消费总额、y=小费，绘制regplot，显示散点、拟合线和95%置信区间 |
| 2    | 分类柱状图         | 按日期分组展示每日平均消费金额，绘制barplot并自动显示误差棒              |
| 3    | 分组小提琴图       | x=日期、y=消费总额、按性别分组，绘制小提琴图，观察分布差异               |
| 4    | 分面网格图         | 行=用餐时段、列=性别，每个子图绘制「日期-消费总额」的箱线图              |
| 5    | 安斯库姆四重奏验证 | 对四组数据分别绘制lmplot，观察回归直线的一致性与数据分布的差异性         |

**参考答案**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
# ---------- 题1：线性回归图 ----------<br />
plt.figure(figsize=(7, 5))<br />
sns.regplot(data=tips, x="total_bill", y="tip", color="#4C72B0",<br />
scatter_kws={"alpha":0.6, "s":30}, line_kws={"linewidth":2})<br />
plt.title("消费金额与小费线性回归")<br />
plt.grid(alpha=0.3)<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题2：分类柱状图 ----------<br />
plt.figure(figsize=(7, 4.5))<br />
sns.barplot(data=tips, x="day", y="total_bill", palette="Set2",
errorbar="sd")<br />
plt.title("每日平均消费金额（带误差棒）")<br />
plt.ylabel("平均消费金额（美元）")<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题3：分组小提琴图 ----------<br />
plt.figure(figsize=(9, 5))<br />
sns.violinplot(data=tips, x="day", y="total_bill", hue="sex",<br />
split=True, palette="Set2", inner="quartile")<br />
plt.title("不同日期、性别的消费分布小提琴图")<br />
plt.ylabel("消费总额（美元）")<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题4：分面网格图 ----------<br />
g = sns.FacetGrid(tips, row="time", col="sex", height=3.5,
margin_titles=True)<br />
g.map(sns.boxplot, "day", "total_bill", order=["Thur", "Fri", "Sat",
"Sun"], palette="Set2")<br />
g.set_axis_labels("日期", "消费总额")<br />
g.fig.suptitle("分时段、分性别消费分布", y=1.02)<br />
plt.tight_layout()<br />
plt.show()<br />
<br />
# ---------- 题5：安斯库姆四重奏验证 ----------<br />
sns.lmplot(data=anscombe, x="x", y="y", col="dataset",
hue="dataset",<br />
col_wrap=2, ci=None, palette="Set2", height=3.5,<br />
scatter_kws={"s": 50, "alpha": 0.8})<br />
plt.suptitle("安斯库姆四重奏：相同回归，不同分布", y=1.02)<br />
plt.tight_layout()<br />
plt.show()</td>
</tr>
</tbody>
</table>

**第四部分 Plotly交互式可视化（第41-43天）**

**第41天 对应知识点：Plotly Express基础交互式图表**

**初始化代码（先执行）**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
import plotly.express as px<br />
import numpy as np<br />
import pandas as pd<br />
<br />
# 函数曲线数据<br />
x = np.linspace(0, 2*np.pi, 100)<br />
df_line = pd.DataFrame({<br />
"x": x,<br />
"sin(x)": np.sin(x),<br />
"cos(x)": np.cos(x)<br />
}).melt(id_vars="x", var_name="函数", value_name="y值")<br />
<br />
# 产品销量数据<br />
df_sales = pd.DataFrame({<br />
"产品": ["产品A", "产品B", "产品C", "产品D", "产品E"],<br />
"销量": [120, 85, 160, 95, 130],<br />
"利润": [30, 20, 45, 25, 35]<br />
})<br />
<br />
# iris数据集<br />
iris = px.data.iris()<br />
gapminder = px.data.gapminder()</td>
</tr>
</tbody>
</table>

**题目列表**

|      |                |                                                                                            |
|------|----------------|--------------------------------------------------------------------------------------------|
| 题号 | 题目类型       | 需求描述                                                                                   |
| 1    | 交互式折线图   | 绘制sin和cos双折线，支持悬停显示坐标、缩放、图例点击显隐，添加标题和轴标签                 |
| 2    | 多维度散点图   | iris数据集绘制散点图，x=花萼宽、y=花萼长，按品种上色，点大小映射花瓣长度，悬停显示全部特征 |
| 3    | 交互式柱状图   | 绘制销量和利润分组柱状图，支持图例点击显隐，悬停显示具体数值                               |
| 4    | 时间动画散点图 | gapminder数据集绘制人均GDP与预期寿命散点图，添加年份动画，点大小映射人口、颜色映射大洲     |

**参考答案**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
# ---------- 题1：交互式折线图 ----------<br />
fig = px.line(df_line, x="x", y="y值", color="函数",<br />
title="正弦与余弦函数对比",<br />
labels={"x": "x值", "y值": "y值"})<br />
fig.update_layout(hovermode="x unified")<br />
fig.show()<br />
<br />
# ---------- 题2：多维度散点图 ----------<br />
fig = px.scatter(iris,<br />
x="sepal_width",<br />
y="sepal_length",<br />
color="species",<br />
size="petal_length",<br />
hover_data=["sepal_length", "sepal_width", "petal_length",
"petal_width"],<br />
title="鸢尾花特征多维度散点图",<br />
labels={"sepal_width": "花萼宽度", "sepal_length": "花萼长度",
"species": "品种"})<br />
fig.show()<br />
<br />
# ---------- 题3：交互式柱状图 ----------<br />
df_sales_melt = df_sales.melt(id_vars="产品", var_name="指标",
value_name="数值")<br />
fig = px.bar(df_sales_melt,<br />
x="产品",<br />
y="数值",<br />
color="指标",<br />
barmode="group",<br />
title="产品销量与利润对比",<br />
text_auto=True)<br />
fig.show()<br />
<br />
# ---------- 题4：时间动画散点图 ----------<br />
fig = px.scatter(gapminder,<br />
x="gdpPercap",<br />
y="lifeExp",<br />
size="pop",<br />
color="continent",<br />
hover_name="country",<br />
animation_frame="year",<br />
animation_group="country",<br />
log_x=True,<br />
size_max=60,<br />
range_x=[100, 100000],<br />
range_y=[25, 90],<br />
title="全球人均GDP与预期寿命变化趋势（1952-2007）",<br />
labels={"gdpPercap": "人均GDP", "lifeExp": "预期寿命", "continent":
"大洲"})<br />
fig.show()</td>
</tr>
</tbody>
</table>

**第42天 对应知识点：Streamlit / Dash 简易仪表盘**

**题目1：Streamlit极简可视化应用**

**需求**：开发单页应用，支持上传CSV文件/使用内置数据集，下拉选择x、y轴字段，切换图表类型，实时渲染交互式图表。

**参考答案（保存为app.py，命令行执行 streamlit run app.py）**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
import streamlit as st<br />
import pandas as pd<br />
import plotly.express as px<br />
<br />
st.set_page_config(page_title="简易数据可视化仪表盘",
layout="wide")<br />
st.title("📊 交互式数据可视化工具")<br />
<br />
# 侧边栏配置<br />
with st.sidebar:<br />
st.header("图表配置")<br />
data_source = st.radio("选择数据源", ["内置Tips数据集",
"上传CSV文件"])<br />
<br />
if data_source == "上传CSV文件":<br />
uploaded_file = st.file_uploader("上传CSV文件", type="csv")<br />
if uploaded_file:<br />
df = pd.read_csv(uploaded_file)<br />
else:<br />
st.info("请上传CSV文件")<br />
df = sns.load_dataset("tips") # 兜底<br />
else:<br />
df = px.data.tips()<br />
<br />
numeric_cols = df.select_dtypes("number").columns.tolist()<br />
x_col = st.selectbox("选择X轴字段", numeric_cols, index=0)<br />
y_col = st.selectbox("选择Y轴字段", numeric_cols, index=1)<br />
chart_type = st.selectbox("选择图表类型", ["折线图", "柱状图", "散点图",
"箱线图"])<br />
<br />
# 主区域渲染图表<br />
col1, col2 = st.columns([3, 1])<br />
with col1:<br />
if chart_type == "折线图":<br />
fig = px.line(df, x=x_col, y=y_col, title=f"{y_col} 随 {x_col}
变化趋势")<br />
elif chart_type == "柱状图":<br />
fig = px.bar(df, x=x_col, y=y_col, title=f"{x_col} 对应 {y_col}
统计")<br />
elif chart_type == "散点图":<br />
fig = px.scatter(df, x=x_col, y=y_col, title=f"{x_col} 与 {y_col}
散点分布")<br />
else:<br />
fig = px.box(df, x=x_col, y=y_col, title=f"{x_col} 分组下 {y_col}
分布")<br />
st.plotly_chart(fig, use_container_width=True)<br />
<br />
with col2:<br />
st.subheader("数据概览")<br />
st.dataframe(df.describe(), use_container_width=True)</td>
</tr>
</tbody>
</table>

**第43天 对应知识点：地图可视化**

**初始化代码（先执行）**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
import plotly.express as px<br />
carshare = px.data.carshare()</td>
</tr>
</tbody>
</table>

**题目列表**

|      |              |                                                                                         |
|------|--------------|-----------------------------------------------------------------------------------------|
| 题号 | 题目类型     | 需求描述                                                                                |
| 1    | 散点地图     | 使用carshare数据集绘制scatter_map，按高峰时段上色，点大小映射车辆时长，设置合适缩放级别 |
| 2    | 分级填色地图 | 模拟世界各国GDP数据，绘制交互式Choropleth地图，颜色深浅对应GDP数值，悬停显示详情        |

**参考答案**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
# ---------- 题1：散点地图 ----------<br />
fig = px.scatter_map(carshare,<br />
lat="centroid_lat",<br />
lon="centroid_lon",<br />
color="peak_hour",<br />
size="car_hours",<br />
color_continuous_scale=px.colors.cyclical.IceFire,<br />
size_max=15,<br />
zoom=10,<br />
hover_name="peak_hour",<br />
title="蒙特利尔共享汽车高峰时段分布")<br />
fig.update_layout(map_style="carto-positron")<br />
fig.show()<br />
<br />
# ---------- 题2：分级填色地图 ----------<br />
# 模拟世界各国GDP数据<br />
countries = ["China", "United States", "Japan", "Germany",
"India",<br />
"United Kingdom", "France", "Brazil", "Italy", "Canada"]<br />
gdp_values = [18000, 26000, 4200, 4500, 3800, 3200, 3000, 2100, 2200,
2000]<br />
df_gdp = pd.DataFrame({"country": countries, "gdp_billion":
gdp_values})<br />
<br />
fig = px.choropleth(df_gdp,<br />
locations="country",<br />
locationmode="country names",<br />
color="gdp_billion",<br />
hover_name="country",<br />
color_continuous_scale="Blues",<br />
title="2026年世界主要国家GDP（十亿美元）",<br />
labels={"gdp_billion": "GDP（十亿美元）"})<br />
fig.show()</td>
</tr>
</tbody>
</table>

**第五部分 综合项目与阶段复习（第44-45天）**

**综合项目：零售销售数据可视化分析报告**

**项目需求**

基于模拟零售销售数据，完成完整的可视化分析，包含核心指标、趋势、品类、分布、关联、热力矩阵6大模块。

**初始化数据代码**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
np.random.seed(2026)<br />
dates = pd.date_range("2026-01-01", "2026-06-30", freq="D")<br />
categories = ["食品", "服装", "家电", "日用品", "美妆"]<br />
regions = ["华东", "华南", "华北", "西南", "华中"]<br />
<br />
# 生成销售明细数据<br />
sales_data = []<br />
for date in dates:<br />
for region in regions:<br />
for cat in categories:<br />
sales = np.random.randint(500, 5000)<br />
orders = np.random.randint(20, 200)<br />
profit = sales * np.random.uniform(0.15, 0.35)<br />
sales_data.append([date, region, cat, sales, orders, profit])<br />
<br />
df_sales = pd.DataFrame(sales_data,<br />
columns=["日期", "地区", "品类", "销售额", "订单量", "利润"])</td>
</tr>
</tbody>
</table>

**完整项目参考代码**

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<tbody>
<tr class="odd">
<td>Python<br />
# ===== 1. 核心指标概览 =====<br />
total_sales = df_sales["销售额"].sum()<br />
total_orders = df_sales["订单量"].sum()<br />
avg_price = total_sales / total_orders<br />
month_growth = 0.085 # 模拟环比<br />
<br />
print(f"总销售额：{total_sales:,.0f} 元")<br />
print(f"总订单量：{total_orders:,} 单")<br />
print(f"平均客单价：{avg_price:.2f} 元")<br />
print(f"月度环比增长率：{month_growth*100:.1f}%")<br />
<br />
# ===== 2. 月度销售趋势 =====<br />
monthly_sales =
df_sales.groupby(df_sales["日期"].dt.to_period("M"))["销售额"].sum().reset_index()<br />
monthly_sales["日期"] = monthly_sales["日期"].astype(str)<br />
<br />
fig = px.line(monthly_sales, x="日期", y="销售额",<br />
title="2026年上半年月度销售额趋势",<br />
markers=True, text="销售额")<br />
fig.update_traces(texttemplate="%{text:,.0f}", textposition="top
center")<br />
fig.show()<br />
<br />
# ===== 3. 品类销售分析 =====<br />
cat_sales = df_sales.groupby("品类")["销售额"].sum().reset_index()<br />
fig1 = px.pie(cat_sales, values="销售额", names="品类",<br />
title="各品类销售额占比", hole=0.4)<br />
fig1.show()<br />
<br />
fig2 = px.bar(cat_sales.sort_values("销售额", ascending=False),<br />
x="品类", y="销售额", title="各品类销量对比", text_auto=True)<br />
fig2.show()<br />
<br />
# ===== 4. 分布分析 =====<br />
daily_sales = df_sales.groupby("日期")["销售额"].sum()<br />
fig = px.histogram(daily_sales, x="销售额", nbins=20,<br />
title="日销售额分布直方图",<br />
marginal="box")<br />
fig.show()<br />
<br />
region_box = df_sales.groupby(["日期",
"地区"])["销售额"].sum().reset_index()<br />
fig = px.box(region_box, x="地区", y="销售额",<br />
title="各地区日销售额分布箱线图")<br />
fig.show()<br />
<br />
# ===== 5. 关联分析 =====<br />
fig = px.scatter(df_sales, x="销售额", y="利润", color="品类",<br />
trendline="ols",<br />
title="销售额与利润关联散点图")<br />
fig.show()<br />
<br />
# ===== 6. 地区×品类热力矩阵 =====<br />
pivot_sales = df_sales.pivot_table(index="地区", columns="品类",<br />
values="销售额", aggfunc="sum")<br />
plt.figure(figsize=(9, 6))<br />
sns.heatmap(pivot_sales, annot=True, fmt=",.0f", cmap="Blues",
linewidths=0.5)<br />
plt.title("各地区分品类销售额热力图")<br />
plt.tight_layout()<br />
plt.show()</td>
</tr>
</tbody>
</table>

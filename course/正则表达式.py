
'''
正则表达式：
1、优先用 Pattern = re.compile(pattern, flags)  编译复杂正则，提升性能（多次使用时）
pattern：处理反斜杠多的正则时，用原始字符串 r'...'
flags：需要忽略大小写时加 re.I，处理多行文本时加 re.M
re.I：忽略大小写
re.M：多行模式（^ $ 匹配每行开头结尾）
re.S：. 匹配包括换行符的任意字符
re.A：\w \d 等仅匹配 ASCII
re.X：忽略空格和注释，方便写复杂正则

2、匹配开头用 match，任意位置用 search，所有匹配用 findall
2.1 Pattern.match(text)
# 只从字符串起始位置匹配，只匹配一次，开头不匹配则返回 None
Match = re.match(r'(\d{4})-(\d{2})-(\d{2})', '2026-06-19')  

Match.group()/group(0)：整个匹配字符串
Match.group(n)：第 n 个分组（方便结构化提取数据）
Match.groups()：所有分组元组（方便用元组解包）
Match.start() / end() / span()：匹配位置

2.2 Pattern.search(text)
re.search('com', 'www.runoob.com')              # 扫描整个字符串，返回第一个匹配成功的结果

2.3 Pattern.findall(text)
re.findall(r'\d+', 'runoob 123 google 456')     # 找到所有匹配，返回列表（多个分组时返回元组列表）

2.3.1 finditer
for match in re.finditer(r"\d+", "12a32bc43"):   # 类似 findall，但返回迭代器（每个元素是 Match 对象）
    print(match.group())

3、替换用 sub，分割用 split
re.sub(r'\D', "", phone)                            # 替换字符串中的匹配项
re.split('\W+', 'runoob, runoob, runoob.')          # 按匹配子串分割字符串，返回列表

def double(matched):
    return str(int(matched.group('number')) * 2)     # 通过名字获取分组内容，不用记编号
re.sub('(?P<number>\d+)', double, 'A23G4HFD567')     # A46G8HFD1134

# (?P<number>\d+)：Python 正则中的「命名分组」
| ------------------------------------------------------ |
| 部分      | 含义                                        |
| ------------------------------------------------------ |
| (?P<...>) | 命名分组的固定格式，P 是 Python 特有的前缀    |
| number    | 你给这个分组起的名字                         |
| \d+       | 实际匹配的内容：一个或多个数字                |
| ------------------------------------------------------ |

# 常用模式语法
| ---------------------------------------------------- |
| 模式         | 含义                                   |
| ---------------------------------------------------- |
| `\w`        | 字母/数字/下划线 `[A-Za-z0-9_]`          |
| `\W`        | 非 `\w`                                 |
| `\d`        | 数字 `[0-9]`                            |
| `\D`        | 非 `\d`                                 |
| `\s`        | 空白字符                                |
| `\S`        | 非 `\s`                                 |
| `\b`        | 单词边界（单词和空格间的位置）             |
| `\B`        | 非 `\b`                                 |
| `[...]`     | 匹配括号内任意一个字符                   |
| `[^...]`    | 匹配不在括号内的字符                     |
| `.`         | 任意字符（除换行符，`re.S` 时包含换行）   |
| `a|b`       | 匹配 a 或 b                             |
| ---------------------------------------------------- |
| `^`         | 字符串开头                              |
| `$`         | 字符串结尾                              |
| `?`         | 0 次或 1 次（非贪婪）                    |
| `+`         | 1 次或多次                              |
| `*`         | 0 次或多次                              |
| `{n}`       | 精确 n 次                               |
| `{n,}`      | 至少 n 次                               |
| `{n,m}`     | n 到 m 次                               |
| ----------------------------------------------------- |
| `(re)`      | 分组                                    |
| `\1`...`\9` | 引用第 n 个分组                          |
| ----------------------------------------------------- |
'''
